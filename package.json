{
  "name": "turupurun-site",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check api/contact.js && node --check scripts/build-cloudflare.js",
    "build:cloudflare:corporate": "node scripts/build-cloudflare.js corporate",
    "build:cloudflare:franchise": "node scripts/build-cloudflare.js franchise"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.2.1",
    "resend": "^6.2.0"
  }
}
